[package]
name = "strongside"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

[build-dependencies]
cc = "1"

[dependencies]
aes = "0.9.1"
cbc = "0.2.1"
rand = "0.10.2"
